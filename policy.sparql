$startid$ (
    <odrl:permission>|(
        <odrl:permission>/(
            <odrl:duty>|(
                <odrl:duty>/(
                    <odrl:constraint>|<odrl:output>|<odrl:target>)
            )
            |<odrl:constraint>|<odrl:output>|<odrl:target>)
    )
    |<odrl:prohibition>|(
        <odrl:prohibition>/(
            <odrl:constraint>|<odrl:output>|<odrl:target>)
    )
)
? ?s ..